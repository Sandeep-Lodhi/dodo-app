{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server.js"
    }
  ],
  "env": {
    "DB_URL": "DB_URL",
    "PORT": "PORT",
    "JWT_SECRET": "JWT_SECRET"
  }
}

